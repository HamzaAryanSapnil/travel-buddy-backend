enum Role {
  USER
  ADMIN
}

enum TravelType {
  SOLO
  COUPLE
  FAMILY
  FRIENDS
  GROUP
}

enum PlanVisibility {
  PUBLIC
  PRIVATE
  UNLISTED
}

enum InvitationStatus {
  PENDING
  ACCEPTED
  DECLINED
  CANCELLED
}

enum MeetupStatus {
  PENDING
  CONFIRMED
  CANCELLED
  COMPLETED
}

enum RatingSource {
  USER_TO_USER
  USER_TO_TRIP
}

enum SubscriptionStatus {
  ACTIVE
  PAST_DUE
  CANCELLED
  EXPIRED
}

enum UserStatus {
  ACTIVE
  SUSPENDED
  DELETED
}

enum TripRole {
  OWNER
  ADMIN
  EDITOR
  VIEWER
}

enum TripStatus {
  JOINED
  PENDING
  REJECTED
}

enum ChatThreadType {
  USER
  PLAN
  MEETUP
  SYSTEM
}

enum NotificationType {
  PLAN_UPDATED
  NEW_MESSAGE
  MEMBER_JOINED
  MEMBER_LEFT
  ITINERARY_ADDED
  ITINERARY_UPDATED
  AI_LIMIT_REACHED
  INVITATION_RECEIVED
  INVITATION_ACCEPTED
  MEETUP_CREATED
  MEETUP_UPDATED
}

