generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// This project uses Prisma Schema Extensions (multi-file schema).
/// Keep your models/enums in sibling *.prisma files under this directory.
/// Prisma CLI merges them automatically when the --schema flag points to the directory.
